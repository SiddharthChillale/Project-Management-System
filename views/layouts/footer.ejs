<footer class="footer py-6">
    <div class="has-text-grey">
        <p class="title is-6 has-text-weight-bold">Project Management System</p>
        <p class="content is-size-7">
            The Project Management System is a robust MVC application designed to streamline the assignment and
            management of projects within development teams. It enables project managers to efficiently oversee the
            relationships between team members and customers who submit projects. The system provides comprehensive
            tools for project tracking, resource allocation, and team collaboration, allowing managers to maintain full
            control over project progress, deliverables, and customer interactions.
        </p>
        <p class="content is-size-7">
            Project Management System is a web application built using
            <span class="has-text-weight-semibold">HTMX, Express, Ejs, and PostgreSQL</span>
        </p>
        <p class="has-text-centered is-size-7 is-family-monospace">
            👨🏽‍🔧 Dev by
            <a href="https://github.com/siddharthchillale">Siddharth C</a>
        </p>
    </div>
    <script>
        document.body.addEventListener('htmx:responseError', function (event) {
            if (event.detail.xhr.status === 401) {
                // Refresh the session
                fetch('http://localhost:3000/api/v1/users/refresh-token', {
                    method: 'POST'
                })
                    .then(response => {
                        if (!response.ok) {
                            throw new Error('Network response was not ok');
                        }
                        // Retry the original request
                        htmx.trigger(event.target, 'htmx:confirm');
                    })
                    .catch(error => {
                        throw new Error(`There has been a problem with refreshing the session: ${error}`);
                    });
            }
        });
    </script>
</footer>