<div>
    <!-- <sl-input type="text" size="small" placeholder="Search for an Event" id="search-input"></sl-input> -->
    <br>
    <sl-select id="events-search-result" name="project[eventId]" size="small" clearable>
        <% for( let ei=0; ei < events.length; ei++ ) { %>
            <sl-option value="<%= events[ei].id %>">
                <%= events[ei].name %>
            </sl-option>
            <%}%>
    </sl-select>
</div>